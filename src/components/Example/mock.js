export const mockImage = [...new Array(5)].map(
  () => "https://cdn.dstv.com/dstvcms/2020/04/16/IS20_100289466_PP_med.jpg"
);

export const ListData = [...new Array(5)].map((e, i) => ({
  title: `Button : ${i + 1}`,
}));
